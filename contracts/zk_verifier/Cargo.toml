[package]
name = "zk-verifier"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { version = "27.0.3", features = ["alloc"] }

[dev-dependencies]
soroban-sdk = { version = "27.0.3", features = ["testutils"] }
